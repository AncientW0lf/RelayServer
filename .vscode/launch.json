{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug web",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/scripts/debug.sh",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "launchBrowser": {
                "enabled": true
            }
        }
    ]
}